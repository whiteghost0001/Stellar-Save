global:
  scrape_interval: 15s
  evaluation_interval: 30s

rule_files:
  - alerts.yml

scrape_configs:
  - job_name: stellar-save-backend
    static_configs:
      - targets: ['backend:3001']
    metrics_path: /metrics
