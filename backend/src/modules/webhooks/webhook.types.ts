export enum WebhookEvent {
  CONTRIBUTION_CREATED = 'contribution.created',
  GROUP_UPDATED = 'group.updated',
  SAVINGS_GOAL_REACHED = 'savings_goal.reached',
  WITHDRAWAL_PROCESSED = 'withdrawal.processed',
}